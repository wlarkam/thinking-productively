# OS / tooling
.DS_Store
.claude/
node_modules/
.vercel/

# Internal design handoff: client brand book + chat logs (not for public repo)
design_handoff/

# PDFs in assets are private: lead-magnet sources (gated behind the opt-in)
# and internal brand/positioning docs (e.g. the Style Guide). Covers (PNGs)
# are published; the source PDFs are not.
assets/*.pdf
